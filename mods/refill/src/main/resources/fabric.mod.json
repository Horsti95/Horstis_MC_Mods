{
	"schemaVersion": 1,
	"id": "horsti_refill",
	"version": "${version}",
	"name": "Horsti Refill",
	"description": "Refills the main hand from the inventory when a stack runs out — server-side, so it works for every player.",
	"authors": ["Horsti", "Claude"],
	"license": "MIT",
	"environment": "*",
	"entrypoints": {
		"main": ["com.horsti.refill.RefillMod"]
	},
	"depends": {
		"fabricloader": ">=0.19.0",
		"fabric-api": "*",
		"minecraft": "~26.2",
		"horsti_core": "*"
	}
}
